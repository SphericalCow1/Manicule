[package]
name = "semtags"
version = "0.6.5"
description = "A local Markdown-first knowledge workspace for notes and tasks using backlinks as semantic tags."
authors = ["Jens"]
license = "AGPL-3.0-only"
repository = "https://github.com/SphericalCow1/Semtags"
homepage = "https://github.com/SphericalCow1/Semtags"
readme = "../README.md"
edition = "2021"
default-run = "Semtags"
autobins = false

[[bin]]
name = "Semtags"
path = "src/main.rs"

[lib]
name = "semtags_lib"
path = "src/lib.rs"
crate-type = ["staticlib", "cdylib", "rlib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tauri = { version = "2", features = [] }
notify = "8"
tauri-plugin-dialog = "2"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[profile.dev]
debug = "line-tables-only"

[profile.dev.package."*"]
debug = false

[profile.release]
debug = false
strip = "symbols"
